dist: trusty
sudo: required

services:
  - docker

env:
  matrix:
    - DISTRO=centos
      VERSION=7

    - DISTRO=ubuntu
      VERSION=trusty

    - DISTRO=ubuntu
      VERSION=wily

    - DISTRO=ubuntu
      VERSION=xenial

    - DISTRO=debian
      VERSION=jessie

script:
  - chmod +x test.sh
  - ./test.sh $DISTRO $VERSION
